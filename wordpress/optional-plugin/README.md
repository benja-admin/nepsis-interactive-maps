# Optional: WordPress plugin route (not in use)

Kept only in case you ever want the shortcode approach instead of the drop-in folder.
It is not needed for the current setup — see `DEPLOY.md` in the project root.

To use it: run `python3 optional-plugin/build_plugin.py` from the project root, then install
the resulting zip via Plugins -> Add New -> Upload Plugin.
